<div id="fba-modal-dialog">
  <h3 id="fba-modal-title">
    Do you have a few minutes to help us test this site?
  </h3>
  <a id="fba-modal-close" type="button" href="#">×</a>
  <p id="fba-description">
    Hi! We’re looking for people to participate in a 30 minute phone interview
    about how they use <%= Organization.first.name %>.
    If selected, we’ll contact you to set up a time.
  </p>
  <form id="recruiter-form" action="/touchpoints/<%= Touchpoint.first.id %>/submissions" method="POST">
    <div id="fba-alert">
      alert message
    </div>
    <fieldset>
      <label for="fba-text-name">First name</label>
      <input id="fba-text-name" name="fba-text-name" class="full-name" type="text" maxlength="500">
      <label for="fba-text-email">Email address</label>
      <input id="fba-text-email" name="fba-text-email" class="email-address" type="email" maxlength="500">
      <label for="fba-text-phone">Phone number</label>
      <input id="fba-text-phone" name="fba-text-phone" class="phone-number" type="tel" maxlength="500">
      <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <div class="button-wrapper">
        <button type="submit" id="fba-submit" class="usa-button-primary-alt" href="#">Send</button>
      </div>
    </fieldset>
  </form>
  <p>
    Note: We won’t share your information with any other organizations or groups, period.
  </p>
  <p id="fba-dialog-privacy" class="usa-external_link">
    <a href="https://www.gsa.gov/portal/content/162010">Privacy</a>
  </p>
</div>
