<fieldset class="radios" role="group" aria-labelledby="<%= question.answer_field %>">
  <h4>
    <%= question_number %>.
    <%= question.text %>
    <span class="usa-label">
      <%= question.answer_field %>
    </span>
  </h4>
  <%= text_field_tag question.answer_field.to_sym %>
</fieldset>
