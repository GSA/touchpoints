<label
  class="usa-label"
  for="<%= question.ui_selector %>">
  <%= question.text %>
  <%- if question.is_required? %>
  <abbr title="required" class="usa-hint--required">*</abbr>
  <% end -%>
  <%= render("components/widget/help_text", question: question) if question.help_text? %>
  <%- if local_assigns[:form_builder] && form_builder %>
  <span class="display-block">
    <span class="usa-tag">
      <%= question.answer_field %>
    </span>
    <%- if question.is_required? %>
    <span class="usa-tag bg-secondary-vivid">
      Required
    </span>
    <% end %>
  </span>
  <% end -%>
</label>
