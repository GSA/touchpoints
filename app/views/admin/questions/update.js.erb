$('.form-ajax-render').on('ajax:success', function(event) { 
    console.log('vp-update');
    $("<%= escape_javascript(render partial: 'components/forms/edit/question', locals: { form: @question.form, question: @question, question_number: 1 }) %>").appendTo("#questions");    
});
