 <%= render partial: "/components/admin/versions",
  locals: {
    title: "Service #{@service.name} versions",
    return_path: admin_service_path(@service),
    export_path: export_versions_admin_service_url(@service,format: "csv")
  }
%>
