<h1>Editing Container</h1>

<%= render 'form', container: @container %>

<%= link_to 'Show', admin_container_path(@container), class: "usa-button" %>
<%= link_to 'Destroy', admin_container_path(@container), method: :delete, data: { confirm: 'Are you sure?' } %>
