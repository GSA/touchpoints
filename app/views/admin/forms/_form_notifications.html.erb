<%= form_with(model: form, url: (form.persisted? ? admin_form_path(form) : admin_forms_path), local: true) do |f| %>
  <% if form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <% form.errors.full_messages.each do |message| %>
        <div class="usa-alert usa-alert--error" >
          <div class="usa-alert__body">
            <h3 class="usa-alert__heading">Error</h3>
            <p class="usa-alert__text">
              <%= message %>
            </p>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="well">
    <div class="grid-row grid-gap-md">
      <div class="grid-col-6">
        <div class="field">
          <%= f.label "emails to notify when a Response is received", class: "usa-label" %>
          <%= f.text_field(:notification_emails, value: (form.notification_emails? ? form.notification_emails : current_user.email), class: "usa-input") %>
        </div>
        <p>
          <%= f.submit "Update Survey", class: "usa-button" %>
        </p>
      </div>
    </div>
  </div>
<% end %>
